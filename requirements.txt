# =============================================================================
# Louisville Weather and Business Risk Monitor — requirements.txt
# =============================================================================
# Install all dependencies with:
#   pip install -r requirements.txt

# --- HTTP requests (Open-Meteo API extraction) ---
requests>=2.31.0

# --- Data transformation and aggregation ---
pandas>=2.1.0

# --- Database connectivity (Supabase / PostgreSQL) ---
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9

# --- Environment variable management (.env credentials) ---
python-dotenv>=1.0.0
